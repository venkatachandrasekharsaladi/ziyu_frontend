/**
 * THE ON/OFF SWITCH for the alternate itinerary design.
 *
 * `false` hides the way in without deleting anything — the screen, its copy and
 * its route all stay on disk, and nothing in the app links to them. That is the
 * setting to use while deciding whether to keep it.
 *
 * To remove it for good, see `README.md` beside this file. Three steps, none of
 * them touching a screen anyone else uses.
 */
export const SHOW_ITINERARY_PREVIEW = true
