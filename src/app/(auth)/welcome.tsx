import { WelcomeScreen } from '@/modules/module-00-auth/screens/WelcomeScreen'

/**
 * Route for M00-S01. Route files stay thin — they map a URL to a screen and
 * nothing else. The implementation lives in the module.
 */
export default WelcomeScreen
